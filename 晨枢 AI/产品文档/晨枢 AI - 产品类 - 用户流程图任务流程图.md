# 晨枢 AI - 产品类 - 用户流程图 / 任务流程图

## 清单

### 7 个步骤

- [x] 立项与产品定位
- [x] 需求分析
- [x] 原型与交互设计
- [x] 技术架构设计
- [x] 开发与工程化
- [x] 测试与上线准备
- [ ] 迭代与产品化

### 12 份文档

- [x] 项目立项文档
- [x] 产品定位与范围说明
- [x] PRD 产品需求文档
- [x] MVP 功能清单与优先级
- [x] 页面原型说明
- [x] 用户流程图 / 任务流程图
- [x] 技术架构设计文档
- [x] 数据库设计文档
- [x] API 接口设计文档
- [x] 开发规范文档
- [x] 部署运行文档
- [x] 测试用例与上线检查清单

---

## 1. 文档目的

本文档用于描述晨枢 AI MVP 阶段的用户流程和任务流程，明确用户如何进入系统、如何配置基础能力、如何创建任务、如何执行任务、如何查看结果、如何复用历史结果。

本文档承接《页面原型说明》，重点从“用户行为路径”和“任务执行链路”的角度解释产品，而不是重复页面结构。

## 2. 流程范围

MVP 阶段重点覆盖以下流程：

- 首次使用与模型配置流程。
- 通用任务创建与执行流程。
- 文件上传与引用流程。
- 简历分析与模拟面试流程。
- 内容创作流程。
- 信息搜集与汇总流程。
- arXiv 论文日报流程。
- 历史任务查看与导出流程。
- 异常与失败处理流程。

首版暂不覆盖：

- 多用户注册和团队协作流程。
- 支付订阅流程。
- 本地模型部署流程。
- 模型微调流程。
- 自动股票交易流程。

## 3. 用户角色与触发场景

### 3.1 个人用户

当前 MVP 的核心用户，也是唯一必需角色。

典型触发场景：

- 想快速开始一个 AI 写作任务。
- 想上传简历并模拟面试。
- 想让系统汇总网页信息。
- 想每天查看某个方向的 arXiv 新论文。
- 想回看历史任务结果并继续修改。

### 3.2 系统

系统负责执行后台动作。

系统行为包括：

- 校验模型配置。
- 解析文件。
- 创建任务。
- 调用模型。
- 抓取网页。
- 拉取 arXiv 数据。
- 保存输出。
- 返回错误提示。

## 4. 总体用户旅程图

```mermaid
journey
    title 晨枢 AI MVP 用户旅程
    section 首次进入
      打开工作台: 3: 用户
      查看模型状态: 3: 用户, 系统
      配置 API Key: 4: 用户
      测试模型连接: 4: 用户, 系统
    section 创建任务
      选择任务类型: 5: 用户
      输入任务内容: 4: 用户
      选择文件和模型: 4: 用户
      提交执行: 5: 用户
    section 等待结果
      查看执行状态: 4: 用户, 系统
      接收流式输出: 5: 用户, 系统
      处理失败重试: 2: 用户, 系统
    section 沉淀复用
      保存结果: 5: 用户, 系统
      导出 Markdown: 4: 用户
      从历史任务继续: 4: 用户
```

## 5. 全局流程总览

```mermaid
flowchart TD
    A[用户打开晨枢 AI] --> B{模型是否已配置}
    B -- 否 --> C[进入模型设置]
    C --> D[填写 API Key / Base URL / 模型名称]
    D --> E{连接测试是否成功}
    E -- 否 --> C
    E -- 是 --> F[返回工作台]
    B -- 是 --> F

    F --> G[选择任务入口]
    G --> H{是否需要文件}
    H -- 是 --> I[上传或选择文件]
    I --> J{文件解析是否成功}
    J -- 否 --> K[查看错误 / 重新解析 / 换文件]
    K --> I
    J -- 是 --> L[填写任务参数]
    H -- 否 --> L

    L --> M[提交任务]
    M --> N[系统执行任务]
    N --> O{执行是否成功}
    O -- 否 --> P[展示失败原因 / 支持重试]
    P --> M
    O -- 是 --> Q[展示结果]
    Q --> R[保存历史]
    R --> S[导出 / 继续追问 / 重新生成]
```

## 6. 首次使用与模型配置流程

### 6.1 流程说明

首次使用时，系统需要先判断是否存在可用模型配置。如果没有配置，用户无法执行 AI 任务，需要先完成模型 API 配置和连接测试。

### 6.2 流程图

```mermaid
sequenceDiagram
    actor U as 用户
    participant W as 工作台
    participant S as 模型设置页
    participant M as 模型服务

    U->>W: 打开晨枢 AI
    W->>W: 检查默认模型配置
    alt 未配置模型
        W-->>U: 展示模型未配置提示
        U->>S: 进入模型设置页
        U->>S: 填写 Provider、Base URL、API Key、模型名称
        U->>S: 点击测试连接
        S->>M: 发起测试调用
        alt 测试成功
            M-->>S: 返回成功
            S-->>U: 展示连接成功
            U->>W: 返回工作台
        else 测试失败
            M-->>S: 返回错误
            S-->>U: 展示失败原因
            U->>S: 修改配置后重试
        end
    else 已配置模型
        W-->>U: 展示可用工作台
    end
```

### 6.3 关键规则

- 未配置模型时，任务执行按钮应不可用或执行前拦截。
- API Key 默认隐藏展示。
- 测试失败时需要展示可理解的错误信息。
- 至少支持 OpenAI 兼容接口。

## 7. 通用任务创建与执行流程

### 7.1 流程说明

通用任务流程是所有业务任务的基础。简历面试、内容创作、信息搜集和 arXiv 日报都可以复用“创建任务、执行任务、保存结果”的底层流程。

### 7.2 流程图

```mermaid
flowchart TD
    A[进入工作台] --> B[点击新建任务]
    B --> C[选择任务类型]
    C --> D[填写任务名称和任务说明]
    D --> E[输入任务内容]
    E --> F{是否关联文件}
    F -- 是 --> G[选择已上传文件或上传新文件]
    G --> H{文件是否解析成功}
    H -- 否 --> I[提示解析失败并允许重试]
    I --> G
    H -- 是 --> J[选择模型和输出格式]
    F -- 否 --> J
    J --> K{必填项是否完整}
    K -- 否 --> L[提示补全字段]
    L --> D
    K -- 是 --> M[提交任务]
    M --> N[创建任务记录]
    N --> O[调用模型或工具]
    O --> P[展示执行中状态]
    P --> Q{执行结果}
    Q -- 成功 --> R[展示 Markdown 结果]
    R --> S[保存历史任务]
    S --> T[复制 / 导出 / 继续追问 / 重新生成]
    Q -- 失败 --> U[展示失败原因]
    U --> V[重试 / 编辑输入 / 返回]
```

### 7.3 任务状态流转图

```mermaid
stateDiagram-v2
    [*] --> 草稿
    草稿 --> 执行中: 用户点击开始执行
    执行中 --> 已完成: 模型或工具返回成功
    执行中 --> 已失败: 模型或工具返回错误
    已失败 --> 执行中: 用户点击重试
    已完成 --> 执行中: 用户点击重新生成
    已完成 --> 已归档: 用户保存或导出
    已归档 --> [*]
```

## 8. 文件上传与引用流程

### 8.1 流程说明

文件流程服务于简历分析、论文写作、专利写作、内容创作和知识沉淀。MVP 阶段重点支持上传、解析、预览和任务引用。

### 8.2 流程图

```mermaid
flowchart TD
    A[进入文件管理页] --> B[上传文件]
    B --> C{文件类型是否支持}
    C -- 否 --> D[提示不支持该格式]
    C -- 是 --> E[保存原文件]
    E --> F[开始解析文本]
    F --> G{解析是否成功}
    G -- 否 --> H[展示解析失败原因]
    H --> I[重新解析 / 删除 / 换文件]
    G -- 是 --> J[保存解析文本]
    J --> K[展示文件预览]
    K --> L{用户下一步}
    L -- 用于任务 --> M[跳转任务创建页并带入文件]
    L -- 继续管理 --> N[返回文件列表]
    L -- 删除文件 --> O[确认删除]
```

### 8.3 文件状态流转

```mermaid
stateDiagram-v2
    [*] --> 已上传
    已上传 --> 解析中
    解析中 --> 解析成功
    解析中 --> 解析失败
    解析失败 --> 解析中: 重新解析
    解析成功 --> 已引用: 被任务使用
    解析成功 --> 已删除: 用户删除
    解析失败 --> 已删除: 用户删除
```

## 9. 简历分析与模拟面试流程

### 9.1 流程说明

简历面试流程是 MVP 的强场景闭环。用户上传简历和岗位 JD，系统先完成分析，再生成面试题，最后根据用户回答形成复盘报告。

### 9.2 用户流程图

```mermaid
flowchart TD
    A[进入简历与模拟面试页] --> B[上传或选择简历]
    B --> C[输入目标岗位 JD]
    C --> D[点击分析简历]
    D --> E[系统解析简历与 JD]
    E --> F[生成简历分析]
    F --> G[展示优势 / 问题 / 修改建议]
    G --> H[生成岗位匹配度]
    H --> I[点击生成面试题]
    I --> J[系统生成题目列表]
    J --> K[用户选择开始面试]
    K --> L[展示当前问题]
    L --> M[用户输入回答]
    M --> N[系统评价回答]
    N --> O{是否还有下一题}
    O -- 是 --> L
    O -- 否 --> P[生成面试复盘报告]
    P --> Q[保存任务结果]
    Q --> R[导出 / 继续练习 / 修改简历]
```

### 9.3 泳道图

```mermaid
flowchart LR
    subgraph U[用户]
        U1[上传简历]
        U2[输入岗位 JD]
        U3[回答面试题]
        U4[查看复盘报告]
    end

    subgraph SYS[晨枢 AI]
        S1[解析简历]
        S2[分析岗位匹配]
        S3[生成面试题]
        S4[评价回答]
        S5[生成复盘]
    end

    subgraph DATA[数据沉淀]
        D1[简历文本]
        D2[岗位信息]
        D3[面试记录]
        D4[复盘报告]
    end

    U1 --> S1 --> D1
    U2 --> S2 --> D2
    S2 --> S3
    S3 --> U3 --> S4 --> D3
    S4 --> S5 --> U4 --> D4
```

### 9.4 输出要求

- 简历结构分析。
- 经历亮点。
- 简历问题。
- 修改建议。
- 岗位匹配度。
- 面试题列表。
- 单题回答评价。
- 总体复盘报告。

## 10. 内容创作流程

### 10.1 流程说明

内容创作流程支持论文、专利、小说、剧本、歌词、小红书、知乎和公众号。推荐采用“先大纲、后正文、再改写”的流程，降低一次生成结果失控的概率。

### 10.2 流程图

```mermaid
flowchart TD
    A[进入内容创作页] --> B[选择内容类型]
    B --> C[输入主题 / 风格 / 篇幅 / 素材]
    C --> D{是否需要上传素材}
    D -- 是 --> E[选择或上传文件]
    E --> F[引用素材内容]
    D -- 否 --> G[进入大纲生成]
    F --> G
    G --> H[生成大纲]
    H --> I{用户是否满意大纲}
    I -- 否 --> J[编辑要求或手动修改大纲]
    J --> H
    I -- 是 --> K[生成正文]
    K --> L[展示正文结果]
    L --> M{后续操作}
    M -- 润色 --> N[生成润色版]
    M -- 扩写 --> O[生成扩写版]
    M -- 缩写 --> P[生成缩写版]
    M -- 改风格 --> Q[生成新风格版本]
    M -- 导出 --> R[导出 Markdown]
    N --> S[保存版本]
    O --> S
    P --> S
    Q --> S
    R --> S
```

### 10.3 不同内容类型差异

| 内容类型 | 关键输入 | 关键输出 |
| --- | --- | --- |
| 论文草稿 | 研究主题、资料、结构要求 | 摘要、大纲、章节草稿 |
| 专利草稿 | 技术方案、创新点、应用场景 | 交底书、权利要求初稿 |
| 小说 | 题材、人物、世界观、风格 | 大纲、章节、人物设定 |
| 剧本 | 题材、角色、场景、冲突 | 分场、对白、剧情节奏 |
| 歌词 | 主题、情绪、风格、押韵 | 主歌、副歌、桥段 |
| 小红书 | 主题、受众、口吻、标签 | 标题、正文、标签 |
| 知乎 | 问题、观点、论据、风格 | 回答结构、正文 |
| 公众号 | 选题、读者、篇幅、结构 | 标题、大纲、长文 |

## 11. 信息搜集与汇总流程

### 11.1 流程说明

信息搜集流程支持 URL 和关键词两种入口。系统需要保留来源，区分原始事实和模型总结。

### 11.2 流程图

```mermaid
flowchart TD
    A[进入信息搜集页] --> B{选择搜集方式}
    B -- URL --> C[输入一个或多个 URL]
    B -- 关键词 --> D[输入关键词和信息源范围]
    C --> E[开始抓取]
    D --> E
    E --> F[系统逐条处理来源]
    F --> G{单个来源是否成功}
    G -- 否 --> H[记录失败原因]
    G -- 是 --> I[提取标题 / 正文 / 时间 / 链接]
    I --> J[生成单来源摘要]
    H --> K{是否还有来源}
    J --> K
    K -- 是 --> F
    K -- 否 --> L[去重与分类]
    L --> M[生成汇总报告]
    M --> N[展示来源列表和报告]
    N --> O[保存任务 / 导出 Markdown / 设置定时任务]
```

### 11.3 报告生成规则

- 必须展示来源链接。
- 需要区分事实摘要和模型分析。
- 抓取失败的来源应显示失败原因。
- 多来源内容需要去重和归类。
- 输出默认使用 Markdown。

## 12. arXiv 论文日报流程

### 12.1 流程说明

arXiv 日报流程用于追踪指定研究方向的最新论文。MVP 阶段可以先支持手动拉取和手动生成日报，后续再接入定时任务。

### 12.2 手动生成流程图

```mermaid
flowchart TD
    A[进入 arXiv 论文日报页] --> B[配置研究方向]
    B --> C[输入关键词 / 排除词 / arXiv 分类]
    C --> D[保存方向配置]
    D --> E[点击手动拉取]
    E --> F[系统请求 arXiv 数据]
    F --> G{是否拉取成功}
    G -- 否 --> H[展示失败原因]
    H --> E
    G -- 是 --> I[论文初筛]
    I --> J[按相关性排序]
    J --> K[调用模型生成中文摘要]
    K --> L[提取贡献 / 方法 / 实验 / 结论]
    L --> M[标记精读推荐]
    M --> N[生成当日日报]
    N --> O[保存历史日报]
    O --> P[查看 / 收藏 / 导出]
```

### 12.3 后续定时流程图

```mermaid
flowchart TD
    A[已启用研究方向] --> B[到达计划时间]
    B --> C[后台自动拉取 arXiv]
    C --> D{拉取是否成功}
    D -- 否 --> E[记录失败日志并通知]
    D -- 是 --> F[筛选相关论文]
    F --> G[生成摘要和推荐]
    G --> H[保存日报]
    H --> I[工作台展示今日论文日报入口]
```

### 12.4 输出要求

- 今日论文概览。
- 推荐精读列表。
- 单篇论文标题。
- 作者。
- 发布时间。
- arXiv 链接。
- 原始摘要。
- 中文摘要。
- 贡献点。
- 方法概述。
- 推荐理由。

## 13. 历史任务查看与导出流程

### 13.1 流程说明

历史任务流程用于保证 AI 产出可追踪、可复用、可导出。它是晨枢 AI 区别于一次性聊天工具的重要流程。

### 13.2 流程图

```mermaid
flowchart TD
    A[进入历史任务页] --> B[查看任务列表]
    B --> C{是否需要筛选}
    C -- 是 --> D[按类型 / 状态 / 时间 / 关键词筛选]
    C -- 否 --> E[选择任务]
    D --> E
    E --> F[进入任务详情页]
    F --> G[查看输入和输出]
    G --> H{用户操作}
    H -- 复制 --> I[复制结果]
    H -- 导出 --> J[导出 Markdown]
    H -- 继续追问 --> K[基于历史任务创建新任务]
    H -- 重新生成 --> L[复用输入并重新执行]
    H -- 删除 --> M[确认删除]
```

## 14. 异常与失败处理流程

### 14.1 流程说明

MVP 阶段必须处理常见失败场景，避免用户不知道下一步该做什么。

### 14.2 失败处理流程图

```mermaid
flowchart TD
    A[任务执行失败] --> B{失败类型}
    B -- 模型未配置 --> C[引导进入模型设置]
    B -- API 调用失败 --> D[展示错误信息并允许重试]
    B -- 文件解析失败 --> E[提示重新解析或更换文件]
    B -- 网页抓取失败 --> F[记录失败来源并继续其他来源]
    B -- arXiv 拉取失败 --> G[提示稍后重试并记录日志]
    B -- 输入缺失 --> H[提示补全必填项]

    C --> I[用户修复后返回任务]
    D --> I
    E --> I
    F --> J[生成部分结果]
    G --> I
    H --> I
```

### 14.3 常见异常清单

| 异常类型 | 用户可见提示 | 推荐操作 |
| --- | --- | --- |
| 模型未配置 | 当前没有可用模型 | 前往模型设置 |
| API Key 错误 | 模型连接失败 | 检查 API Key 和 Base URL |
| 文件格式不支持 | 暂不支持该文件类型 | 更换 Markdown、TXT、PDF、DOCX |
| 文件解析失败 | 文件解析失败 | 重新解析或更换文件 |
| 网页抓取失败 | 该来源抓取失败 | 查看失败原因或跳过 |
| arXiv 请求失败 | 论文拉取失败 | 稍后重试 |
| 输入为空 | 请补全必填内容 | 返回表单补充输入 |

## 15. MVP 端到端闭环

```mermaid
flowchart LR
    A[配置模型] --> B[上传或输入资料]
    B --> C[创建任务]
    C --> D[执行任务]
    D --> E[查看结果]
    E --> F[保存历史]
    F --> G[导出或复用]
    G --> C
```

## 16. 流程验收标准

本流程文档对应的 MVP 验收标准如下：

1. 用户首次进入系统时，可以明确知道是否需要配置模型。
2. 用户可以从工作台进入所有 P0 任务流程。
3. 用户可以完成通用任务创建、执行、保存和导出。
4. 用户可以完成文件上传、解析和任务引用。
5. 用户可以完成简历分析、模拟面试和复盘。
6. 用户可以完成内容大纲、正文生成和改写。
7. 用户可以完成 URL 或关键词信息汇总。
8. 用户可以完成 arXiv 方向配置和日报生成。
9. 用户可以从历史任务继续追问或重新生成。
10. 常见失败场景有明确提示和下一步操作。

## 17. 后续衔接

本流程文档完成后，下一步建议进入《技术架构设计文档》。

技术架构文档需要根据本文流程进一步明确：

- 前端页面与路由结构。
- 后端服务模块。
- 任务执行引擎。
- 模型调用服务。
- 文件解析服务。
- 网页抓取服务。
- arXiv 拉取服务。
- 数据库存储结构。
- 异步任务和日志机制。
